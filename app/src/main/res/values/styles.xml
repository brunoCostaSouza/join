<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimary</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowContentOverlay">@color/colorPrimary</item>
        <item name="android:windowBackground">@color/colorBackground</item>
        <item name="android:textColor">#848484</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppCompat.TelaCheia" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" >
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowContentOverlay">@color/colorPrimary</item>
        <item name="android:windowBackground">@color/colorBackground</item>
        <item name="android:textColor">#848484</item>
    </style>

    <style name="MyProgressBar" parent="@style/Widget.AppCompat.ProgressBar.Horizontal">
        <item name="android:progressBackgroundTint">@color/despesa</item>
        <item name="android:progressTint">@color/receita</item>
        <item name="android:minWidth">200dp</item>
    </style>

    <style name="ThemeSwitchReceita">
        <item name="colorControlActivated">@color/receita</item>
        <item name="colorSwitchThumbNormal">@color/gray_dark</item>
        <item name="android:colorForeground">@color/gray_dark</item>
    </style>
    <style name="ThemeSwitchDespesa">
        <item name="colorControlActivated">@color/despesa</item>
        <item name="colorSwitchThumbNormal">@color/gray_dark</item>
        <item name="android:colorForeground">@color/gray_dark</item>
    </style>

    <style name="DatePickerThemeReceita" parent="Theme.AppCompat.Light.Dialog">
        <item name="colorPrimary">@color/receita</item>
        <item name="colorPrimaryDark">@color/receita</item>
        <item name="colorAccent">@color/receita</item>
    </style>

    <style name="DatePickerThemeDespesa" parent="Theme.AppCompat.Light.Dialog">
        <item name="colorPrimary">@color/despesa</item>
        <item name="colorPrimaryDark">@color/despesa</item>
        <item name="colorAccent">@color/despesa</item>
    </style>

    <style name="DatePickerThemeAccent" parent="Theme.AppCompat.Light.Dialog">
        <item name="colorPrimary">@color/colorAccent</item>
        <item name="colorPrimaryDark">@color/colorAccent</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Full screen dialog -->
    <style name="FullScreenDialogStyle" parent="Theme.AppCompat.Dialog">

        <item name="android:windowNoTitle">true</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorPrimary">@color/colorPrimary</item>

        <!-- Set this to true if you want Full Screen without status bar -->
        <item name="android:windowFullscreen">false</item>

        <item name="android:windowIsFloating">false</item>

        <!-- This is important! Don't forget to set window background -->
        <item name="android:windowBackground">@color/white</item>

        <!-- Additionally if you want animations when dialog opening -->
        <item name="android:windowEnterAnimation">@anim/slide_up</item>
        <item name="android:windowExitAnimation">@anim/slide_down</item>
    </style>

</resources>
