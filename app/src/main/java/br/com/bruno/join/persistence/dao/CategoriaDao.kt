package br.com.bruno.join.persistence.dao

/**
 * Created by Bruno Costa on 10/08/2018.
 */
//@Dao
interface CategoriaDao{
/*
    @Insert(onConflict = OnConflictStrategy.REPLACE)
    fun insert(categoria: Categoria)

    @Insert(onConflict = OnConflictStrategy.REPLACE)
    fun insert(categorias: List<Categoria>)

    @Update
    fun update(categoria: Categoria)

    @Delete
    fun remove(categoria: Categoria)

    @Query("SELECT * FROM Categoria WHERE id_categoria = :tId ")
    fun findById(tId: Long): Categoria

    @Query("SELECT * FROM Categoria")
    fun findAll(): List<Categoria>

    @Query("SELECT * FROM Categoria")
    fun findAllCategories(): List<Categoria>
    */
}